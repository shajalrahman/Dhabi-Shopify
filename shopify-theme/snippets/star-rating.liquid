{% comment %}
  Renders star rating SVG icons.
  Accepts:
  - rating: {Number} The rating value from 1-5 (default: 5)
{% endcomment %}
{%- assign rating_value = rating | default: 5 -%}
{%- for i in (1..rating_value) -%}
  {% render 'icon-svg', icon: 'star' %}
{%- endfor -%}
